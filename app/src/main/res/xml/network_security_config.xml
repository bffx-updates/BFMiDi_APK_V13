<?xml version="1.0" encoding="utf-8"?>
<!-- A API do pedal e HTTP local e o IP STA varia, portanto nao cabe numa lista
     estatica de dominios. O nativo so entrega ao editor hosts cujo /ping prova
     product=BFMIDI; downloads de update continuam HTTPS. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
